import { BusinessConfigPage } from "@/src/components/feature/business-config";
export default async function BusinessLocationsPage({ params }: { params: Promise<{ businessId: string }> }) { const { businessId } = await params; return <BusinessConfigPage kind="locations" businessId={businessId} />; }