import { BusinessConfigPage } from "@/src/components/feature/business-config";
export default async function BusinessHoursPage({ params }: { params: Promise<{ businessId: string }> }) { const { businessId } = await params; return <BusinessConfigPage kind="hours" businessId={businessId} />; }